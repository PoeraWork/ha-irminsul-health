{
  "config": {
    "abort": {
      "already_configured": "This health profile is already configured.",
      "reconfigure_successful": "The health profile was updated.\n\nWebhook URL: `{webhook_url}`\n\nIngest token: `{ingest_token}`"
    },
    "create_entry": {
      "default": "The health profile is ready. Save both credentials now; they work like passwords and are not shown as entities.\n\nWebhook URL: `{webhook_url}`\n\nIngest token: `{ingest_token}`\n\nSend JSON with method POST and the header `Authorization: Bearer <ingest token>`."
    },
    "error": {
      "invalid_subject_id": "Use lowercase letters, numbers, underscores, or hyphens, starting with a letter or number.",
      "required": "This field is required."
    },
    "step": {
      "reconfigure": {
        "data": {
          "allow_remote": "Allow remote requests",
          "rotate_webhook": "Rotate webhook credentials",
          "subject_name": "Display name"
        },
        "data_description": {
          "allow_remote": "Keep this disabled when the sender can reach Home Assistant on the local network or through a VPN.",
          "rotate_webhook": "The old URL and ingest token stop working after the integration reloads."
        },
        "description": "Update the profile or rotate its private credentials.\n\nWebhook URL: `{webhook_url}`\n\nIngest token: `{ingest_token}`",
        "title": "Update health profile"
      },
      "user": {
        "data": {
          "allow_remote": "Allow remote requests",
          "subject_id": "Stable person ID",
          "subject_name": "Display name"
        },
        "data_description": {
          "allow_remote": "Keep this disabled when the sender uses the local network or a VPN.",
          "subject_id": "A stable internal ID such as user. It cannot be changed later.",
          "subject_name": "The person's name shown in Home Assistant."
        },
        "description": "Create a health profile and two private webhook credentials for one person. Local requests are safer and are the default.",
        "title": "Create health profile"
      }
    }
  },
  "entity": {
    "sensor": {
      "uric_acid": {
        "name": "Uric acid"
      },
      "uric_acid_measured_at": {
        "name": "Uric acid measured at"
      }
    }
  }
}
